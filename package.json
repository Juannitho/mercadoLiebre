{
  "name": "desafio",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "ui": "browser-sync start --proxy localhost:3010 --files=**/*  --ignore=node_modules --reload-delay 500 --no-ui --no-inject-changes --port 3010 --browser \"google chrome\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan Castañeda",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "nodemon": "^2.0.7"
  }
}
